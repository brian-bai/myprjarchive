<h1>Sign up</h1>

<%= form_for(@user) do |f| %>
  <%= render "fields", :f => f %>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>
<p>Already have an account? <%= link_to "Sign in now!", signin_path %></p>
